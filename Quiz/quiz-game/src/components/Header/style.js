import styled from "styled-components"
export const Container = styled.div`
   display: flex;
   align-items: center;
   justify-content: space-between;

   height: 10.0rem;
   width: 100%;
   padding: 2.4rem;
   background: #202020;
   color: white;
 
   button {
    background: none;
    border: none;
    color:white;
   }; 

   .menuIcon {
    font-size: 2.4rem;
    color: #FAFAA5;
   };
`